# Text Editor from Scratch in C

## Setup

1. Install C Compiler (GCC) and Make program (assuming Ubuntu OS)
- sudo apt-get install gcc make
2. Check version of C Compiler and Make
- cc --version
- make --version
